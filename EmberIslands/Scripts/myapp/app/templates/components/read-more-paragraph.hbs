    <div class="col-md-4">
        <h2>{{title}}</h2>
        <p>
            {{previewText}}
        </p>
        {{#if isExpanded}}
        <p>
            {{expandedText}}
        </p>
        {{/if}}
        <p><button class="btn btn-link" {{action "toggleReadMore"}}>Read more</button></p>
    </div>